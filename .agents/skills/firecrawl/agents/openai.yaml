interface:
  display_name: "Firecrawl"
  short_description: "Repo-specific patterns and workflows for firecrawl"
  default_prompt: "Use the firecrawl repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true